{
  "name": "monorepo",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "module/spacetimedb",
    "app"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
